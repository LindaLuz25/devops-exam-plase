events { worker_connections 1024 }

http {
    upstream app {
        server app-blue:3000;
        # server app-green:3000; # No lo pongas aquí si solo quieres usar uno a la vez
    }

    server {
        listen 80;
        location / {
            proxy_pass http://app;
        }
    }
}